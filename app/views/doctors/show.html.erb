<h1><%= @doctor.name %></h1>

<h3>Department: <%= @doctor.department %></h3>

<% @doctor.appointments.each do |app| %>
  <%= link_to app.patient.name, patient_path(app.patient) %> - <%= app.appointment_datetime.strftime('%B %d, %Y at %H:%M') %>
<% end %>
